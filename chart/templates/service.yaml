apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-alert-readiness.fullname" . }}
  labels:
    {{- include "prometheus-alert-readiness.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-alert-readiness.selectorLabels" . | nindent 4 }}
